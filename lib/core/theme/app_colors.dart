import 'package:flutter/material.dart';

class AppColors {
  // Primary Backgrounds
  static const Color background = Color(0xFFEBEBEB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceGlass = Color(0x1A000000);

  // Accents
  static const Color primary = Color(0xFFFFD500); // Yellow accent
  static const Color primaryDark = Color(0xFFE6C000);
  
  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textInverse = Color(0xFFFFFFFF);

  // Borders
  static const Color border = Color(0x1A000000); // Faint dark border
  
  // States
  static const Color error = Color(0xFFFF4C4C);
  static const Color success = Color(0xFF4CAF50);
}
